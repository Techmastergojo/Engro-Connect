import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coordinate.helper',
  appName: 'Coordinate Helper',
  webDir: 'dist'
};

export default config;
